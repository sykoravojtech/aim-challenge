# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets and local config
.env
credentials.json
*.pem

# Local data — raw/aims/digests are runtime artefacts. data/compare/ is
# intentionally NOT listed: phase before/after snapshots are committed
# as interview exhibits.
data/raw/
data/aims/
data/digests/
# Eval runs are timestamped + regenerable; data/evals/SUMMARY.md is the curated
# exhibit and stays committed. Ignore the per-run JSON + Markdown traces.
data/evals/run_*.json
data/evals/run_*.md

# Scratch / reference material
tmp/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
